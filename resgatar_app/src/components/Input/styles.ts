import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    width: "100%",
    height: 52,
    backgroundColor: "#121214",
    borderRadius: 10,
    padding: 12,
    fontSize: 16,
    borderWidth: 1,
    margin: 20,
    color: "#FFFFFF",
  },
});
